import { Scene } from "@babylonjs/core/scene"
import { Mesh } from "@babylonjs/core/Meshes/mesh"
import { RenderTargetTexture } from "@babylonjs/core/Materials/Textures/renderTargetTexture"
import { Vector3 } from "@babylonjs/core/Maths/math"
import { StandardMaterial } from "@babylonjs/core/Materials/standardMaterial"

export const createRetro = (scene: Scene) => {
//     let plane = Mesh.CreateGround('test', 6, 6, 2, scene)
//    // plane.rotation.z = Math.PI/2
//     plane.position=new Vector3(0,10,0)
//     let text = new RenderTargetTexture('tex', 512, scene)
//     text.activeCamera = scene.activeCameras[1]
//     console.log(scene.meshes)
//     scene.meshes.forEach((msh, i) =>{
//         if (msh != plane){text.renderList.push(msh)}
//     })
    
//     text.uScale = -1
//     let mat = new StandardMaterial('mat', scene)
//     mat.diffuseTexture = text
//     plane.material = mat
//     console.log(plane)
    
// 	scene.registerBeforeRender(() => {
// 		text.render();
//     })
    
}
