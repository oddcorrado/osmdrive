//import '@babylonjs/core/Loading/Plugins';
import  '@babylonjs/loaders/OBJ'
import {SceneLoader} from '@babylonjs/core/Loading/sceneLoader'
import { Vector3 } from '@babylonjs/core/Maths/math';

export default function dressMap(scene){
var i = 0;
var z = 0;
var tree = []/*
for (var x = 0; x < 10; x++)
    tree.push(SceneLoader.ImportMesh('', "../mesh/Tree/", "Tree.obj", scene, function (newMesh) {
        newMesh.forEach((mesh, i = 0, y= 0) => {
            mesh.position = new Vector3(x +10, x+10, x+10)
        })
        console.log('made', newMesh.position)
    })
    )*///work in progress

}