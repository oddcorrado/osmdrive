import { Scene } from "@babylonjs/core/scene";
import { FreeCamera } from "@babylonjs/core/Cameras/freeCamera";
import { Engine } from "@babylonjs/core/Engines/engine";


export const setupMirror = (engine: Engine, camera: FreeCamera) => {
  //  engine.registerView(document.getElementById('centralmirror'), camera)

    //let test = new Engine(document.getElementById("centralmirror"))
   // let view = engine.registerView(document.getElementById("centralmirror"), camera);
}